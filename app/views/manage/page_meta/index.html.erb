<div class="row">
  <div class="col-lg-12">
    <table class="table table-bordered table-hover table-condensed">
      <thead>
        <th>Название</th>
      </thead>
      <tbody>
        <% @page_metas.each do |page_meta| %>
          <tr>
            <td><%= link_to I18n.t("page_meta.#{page_meta.slug}"), edit_manage_page_metum_path(page_meta) %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>
